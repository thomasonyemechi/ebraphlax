ALTER TABLE `stocks` CHANGE `net_weight` `net_weight` DOUBLE(20,2) NULL DEFAULT NULL, CHANGE `gross_weight` `gross_weight` DOUBLE(20,2) NULL DEFAULT NULL, CHANGE `moisture_discount` `moisture_discount` DOUBLE(20,2) NOT NULL DEFAULT '0', CHANGE `price` `price` DOUBLE(20,2) NULL DEFAULT NULL, CHANGE `bags` `bags` DOUBLE(20,2) NULL DEFAULT NULL, CHANGE `total` `total` DOUBLE(20,2) NULL DEFAULT NULL, CHANGE `current_balance` `current_balance` DOUBLE(20,2) NULL DEFAULT NULL;


ALTER TABLE `sales_summaries` ADD `gross_weight` DOUBLE(20,2) NULL AFTER `sales_price`, ADD `net_weight` DOUBLE(20,2) NULL AFTER `gross_weight`, ADD `moisture_discount` DOUBLE(20,2) NULL AFTER `net_weight`, ADD `bags` DOUBLE(20,2) NULL AFTER `moisture_discount`, ADD `tares` DOUBLE(20,2) NULL AFTER `bags`;
